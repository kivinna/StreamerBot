# StreamerBot
This bot will post when someone is streaming with a custom message (much like Live Bot by Squidsomething, will edit this later), and then will delete the message when the stream is over.
